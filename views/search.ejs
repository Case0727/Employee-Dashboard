<% if (User.length > 0) { %>
    <h2>Search Results</h2>
    <ul>
        <% User.forEach(user => { %>
            <li><%= User._id %> - <%= User.name %> - <%= User.email %> - <%= User.status %></li>
        <% }) %>
    </ul>
<% } else { %>
    <p>No users found.</p>
<% } %>



